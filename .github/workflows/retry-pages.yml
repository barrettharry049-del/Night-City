name: Retry failed Rainline City publish disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Disabled. The site no longer uses action-based Pages deployments."
